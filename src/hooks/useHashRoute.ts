/**
 * @deprecated Použij useRoute z "./useRoute". Aplikace přešla z hash routingu
 * na path routing (History API). Tento soubor je tu jen jako backwards-compat
 * alias — smaž ho, až odstraníš všechny importy `useHashRoute`.
 */
export { useRoute as useHashRoute } from "./useRoute";
